1. Comprension del ejemplo: En este ejemplo se desarrolla un escaner que reporta el numero de los caracteres, las palabras y las lineas de un archivo

2. Ejecucion del ejemplo: Ejecutado correctamente

3. Analisis de la salida: Cuando se ejecuta el escaner le escribimos un pequeño input, este nos mostrara el numero de caracteres, palabras y lineas del input que le agregamos.

4. Hallazgos: En el desarrollo de este ejemplo me di cuenta que es necesario agregar el tipo de retorno explicitamente (int) en la funcion main ya que en el C moderno (C99 en adelante) no se puede omitir porque arrojara error en la compilacion.
